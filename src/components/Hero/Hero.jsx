import { Wrapper, Container, Title } from "./Hero.styled";

const Hero = () => {
    return(
        <Wrapper>
            <Container>
                <Title>Welcome to the home page</Title>
            </Container>
        </Wrapper>
    );
};

export default Hero;